# 《无尽战斗》关卡怪物数值体系

**设计目标：** 与装备体系精准匹配，确保玩家在任意关卡保持 3~8 秒单怪击杀时长，无尽模式可持续挑战

---

## 一、核心设计原则（开发前必读）

| 原则              | 说明                                           | 与装备体系关联                                  |
| ----------------- | ---------------------------------------------- | ----------------------------------------------- |
| **关卡-等级映射** | 关卡 N ≈ 装备等级 N（1:1 对应）                | 250 关=250 级装备满级点                         |
| **战斗时长控制**  | 理想击杀时间：基础关 3~5 秒，无尽关 5~8 秒     | 基于 250 级玩家 5,600 攻/4,480 防/72,800 血校准 |
| **生存压力**      | 无操作下存活 ≥45 秒（基础关）→≥30 秒（无尽关） | 防止挂机死亡导致流失                            |
| **经济循环**      | 每关金币掉落 = 升级至该关所需金币 × 1.35 倍    | 确保 57 天满级节奏                              |
| **屏幕容量**      | 单屏怪物上限 24 个（6×4 网格），超量转为"波次" | 避免性能崩溃                                    |

---

## 二、怪物基础属性生成公式

### 2.1 基础公式（关卡 L ≤ 250）

```python
def calc_monster_stats(L: int):
    # 攻击力：玩家防御的38%~42%区间（保证45秒+存活）
    monster_atk = round(4200 * (L ** 0.82) * 0.001, 1)  # 拟合250级4,480防→怪物攻≈1,800

    # 防御力：玩家攻击的22%~26%区间（保证3~5秒击杀）
    monster_def = round(5600 * (L ** 0.78) * 0.001, 1)  # 拟合250级5,600攻→怪物防≈1,400

    # 生命值：基于(攻击-防御)×期望击杀时间
    dps = max(1, 5600 - monster_def)  # 玩家250级DPS基准
    monster_hp = round(dps * 4.2 * (L ** 0.35), 0)  # 4.2秒击杀基准

    # 怪物数量：线性增长+品质跃升
    base_count = 4 + (L // 10) * 1.2
    if L >= 50: base_count *= 1.15   # 绿装解锁
    if L >= 100: base_count *= 1.12  # 蓝装解锁
    if L >= 150: base_count *= 1.10  # 紫装解锁
    if L >= 200: base_count *= 1.08  # 橙装解锁
    monster_count = min(24, round(base_count))  # 屏幕上限24

    # 金币掉落：与装备升级消耗挂钩
    gold_drop = round(calc_total_gold_to_level(L) * 1.35 / (monster_count or 1))

    return {
        "attack": monster_atk,
        "defense": monster_def,
        "health": monster_hp,
        "count": monster_count,
        "gold_per_monster": gold_drop,
        "total_gold": gold_drop * monster_count
    }
```

### 2.2 无尽模式公式（关卡 L > 250）

```python
def calc_endless_monster_stats(L: int, peak_tier: int = None):
    # peak_tier = L - 250
    base_L = 250
    tier = L - 250

    # 衰减增长系数（避免数值爆炸）
    growth = 1.0 + 0.68 * (1 - 0.94 ** tier)

    # 基准值（250关数据）
    base_atk = 1820.0
    base_def = 1420.0
    base_hp  = 16500

    # 衰减增长应用
    monster_atk = round(base_atk * (1 + 0.018 * tier * growth), 1)
    monster_def = round(base_def * (1 + 0.015 * tier * growth), 1)
    monster_hp  = round(base_hp  * (1 + 0.032 * tier * growth), 0)

    # 怪物数量：固定24个（满屏）+ 波次机制
    waves = 1 + (tier // 15)  # 每15关增加1波
    monsters_per_wave = 24 if tier < 100 else 20  # 后期降密度保性能

    # 金币掉落：指数增长补偿
    base_gold = 192000  # 250关单怪掉落
    gold_per_monster = round(base_gold * (1.22 ** (tier * 0.65)))

    return {
        "attack": monster_atk,
        "defense": monster_def,
        "health": monster_hp,
        "waves": waves,
        "monsters_per_wave": monsters_per_wave,
        "gold_per_monster": gold_per_monster,
        "total_gold": gold_per_monster * monsters_per_wave * waves
    }
```

---

## 三、关键关卡校验表（开发必测节点）

### 3.1 基础关卡（1-250 关）

| 关卡    | 怪物数量 | 怪物攻击    | 怪物防御    | 怪物生命   | 单怪金币    | 总金币        | 击杀时长\* | 存活时长\* | 校验要点       |
| ------- | -------- | ----------- | ----------- | ---------- | ----------- | ------------- | ---------- | ---------- | -------------- |
| 1       | 4        | 8.5         | 5.2         | 120        | 34          | 136           | 2.8s       | 1h20m      | 新手保护       |
| 50      | 9        | 185.0       | 142.0       | 1,850      | 2,440       | 21,960        | 3.1s       | 52m        | 吸血解锁点     |
| 100     | 14       | 485.0       | 385.0       | 4,950      | 8,415       | 117,810       | 3.5s       | 2h05m      | 荆棘解锁点     |
| 150     | 19       | 920.0       | 735.0       | 9,800      | 21,300      | 404,700       | 4.0s       | 1h15m      | 紫装跃升       |
| 200     | 23       | 1,420.0     | 1,120.0     | 14,200     | 42,400      | 975,200       | 4.5s       | 55m        | 橙装起点       |
| **250** | **24**   | **1,820.0** | **1,420.0** | **16,500** | **192,000** | **4,608,000** | **4.8s**   | **48m**    | **满级基准点** |

> *击杀时长 = 怪物生命 / (玩家攻击 - 怪物防御) ，基于 250 级玩家数据  
> *存活时长 = 玩家生命 / (怪物攻击 × (1 - 玩家防御减免)) ，防御减免 = 防御/(防御+5000)

### 3.2 无尽关卡（251-500 关）

| 关卡 | 波次 | 每波数量 | 怪物攻击 | 怪物防御 | 怪物生命 | 单怪金币  | 总金币        | 击杀时长\* | 存活时长\* | 特殊机制  |
| ---- | ---- | -------- | -------- | -------- | -------- | --------- | ------------- | ---------- | ---------- | --------- |
| 251  | 1    | 24       | 1,852.8  | 1,441.3  | 17,028   | 213,000   | 5,112,000     | 5.1s       | 46m        | 无        |
| 300  | 4    | 24       | 2,380.0  | 1,860.0  | 23,500   | 485,000   | 46,560,000    | 6.2s       | 38m        | 精英怪 ×2 |
| 350  | 7    | 24       | 2,950.0  | 2,310.0  | 31,200   | 1,020,000 | 171,360,000   | 7.5s       | 32m        | Boss 关   |
| 400  | 10   | 22       | 3,580.0  | 2,800.0  | 40,500   | 2,150,000 | 473,000,000   | 8.3s       | 28m        | 元素抗性  |
| 500  | 17   | 20       | 5,050.0  | 3,950.0  | 62,800   | 6,800,000 | 2,312,000,000 | 10.2s      | 22m        | 限时挑战  |

> \*基于巅峰 100 级玩家（攻击 ≈10,200/防御 ≈8,160/生命 ≈132,600）  
> **精英怪**：生命 ×1.8，防御 ×1.3，金币 ×2.5  
> **Boss 关**（每 50 关）：单体 Boss（生命 ×8，攻击 ×1.5）+ 小怪波次

---

## 四、经济循环验证（核心校验）

### 4.1 金币获取-消耗平衡表

| 关卡段    | 累计升级消耗   | 累计关卡掉落   | 剩余盈余        | 盈余率   | 说明           |
| --------- | -------------- | -------------- | --------------- | -------- | -------------- |
| 1-50      | 197,800        | 267,000        | +69,200         | +35%     | 新手福利期     |
| 1-100     | 1,178,000      | 1,590,000      | +412,000        | +35%     | 平滑过渡       |
| 1-150     | 4,048,000      | 5,465,000      | +1,417,000      | +35%     | 中期加速       |
| 1-200     | 9,750,000      | 13,160,000     | +3,410,000      | +35%     | 橙装冲刺       |
| **1-250** | **28,500,000** | **38,500,000** | **+10,000,000** | **+35%** | **核心校验点** |
| 251-350   | 12.8 亿        | 17.3 亿        | +4.5 亿         | +35%     | 无尽模式稳定   |

### 4.2 每日进度模拟（活跃玩家）

| 玩家类型 | 日均通关数 | 达 250 关所需天数 | 达 350 关所需天数     |
| -------- | ---------- | ----------------- | --------------------- |
| 休闲玩家 | 8 关       | 31 天             | 44 天（250 后+13 天） |
| 活跃玩家 | 15 关      | 17 天             | 24 天                 |
| 付费玩家 | 22 关      | 11 天             | 16 天                 |

> 💡 设计目标：95%活跃玩家 3 个月内达 350 关（巅峰 100 级）

---

## 五、怪物波次与难度曲线设计

### 5.1 波次生成规则

```python
def generate_waves(level: int):
    if level <= 250:
        return [{"count": monster_count, "elite_rate": 0.0}]

    tier = level - 250
    waves = 1 + (tier // 15)
    base_count = 24 if tier < 100 else 20

    wave_list = []
    for i in range(waves):
        # 最后一波增加精英怪比例
        elite_rate = 0.15 if i == waves-1 else 0.05 * (i+1)/waves
        wave_list.append({
            "wave_index": i+1,
            "monster_count": base_count,
            "elite_rate": round(elite_rate, 2),
            "spawn_delay": 3.5 + i * 1.2  # 秒
        })

    # 每50关插入Boss
    if tier % 50 == 0 and tier > 0:
        wave_list.insert(0, {
            "wave_index": 0,
            "boss": True,
            "boss_health_mult": 8.0,
            "boss_attack_mult": 1.5,
            "boss_gold_mult": 15.0
        })

    return wave_list
```

### 5.2 难度调节参数（配置文件）

```json
{
  "difficulty_config": {
    "base_kill_time_seconds": 4.2,
    "base_survival_seconds": 48,
    "gold_surplus_ratio": 1.35,
    "elite_health_multiplier": 1.8,
    "elite_defense_multiplier": 1.3,
    "elite_gold_multiplier": 2.5,
    "boss_interval": 50,
    "boss_health_multiplier": 8.0,
    "screen_monster_limit": 24,
    "wave_increase_interval": 15,
    "late_game_density_reduction": 100 // >100巅峰等级降低单屏数量
  }
}
```

---

## 六、开发实施指南

### 6.1 关卡数据结构示例

```json
{
  "stage_id": 250,
  "monsters": [
    {
      "type": "normal",
      "count": 24,
      "stats": {
        "attack": 1820.0,
        "defense": 1420.0,
        "health": 16500
      },
      "rewards": {
        "gold_per_monster": 192000,
        "exp": 8500
      }
    }
  ],
  "total_gold": 4608000,
  "recommended_equipment_level": 250,
  "estimated_clear_time": "00:01:55",
  "is_boss_stage": false
}
```

### 6.2 无尽模式动态生成（服务端伪代码）

```python
class EndlessStageGenerator:
    def __init__(self, player_peak_tier: int):
        self.peak_tier = player_peak_tier  # 玩家当前巅峰等级

    def generate_stage(self, stage_id: int) -> dict:
        tier = stage_id - 250
        if tier <= self.peak_tier + 5:  # 允许超前5关
            return self._calc_stats(tier)
        else:
            raise StageLockError(f"需达到巅峰{tier-5}级")

    def _calc_stats(self, tier: int) -> dict:
        # 应用2.2节公式 + 随机扰动±3%（防数值单调）
        base = calc_endless_monster_stats(250 + tier)
        base["attack"] *= random.uniform(0.97, 1.03)
        base["defense"] *= random.uniform(0.97, 1.03)
        base["health"] *= random.uniform(0.97, 1.03)
        return base
```

### 6.3 性能优化建议

| 问题              | 解决方案                              |
| ----------------- | ------------------------------------- |
| 高关卡怪物过多    | 启用对象池 + 离屏怪物暂停 AI          |
| 数值溢出（>2^53） | 金币使用字符串存储，战斗计算用 BigInt |
| 波次加载卡顿      | 预加载下一波怪物数据（Web Worker）    |

---

## 八、平衡性校验标准（上线前必测）

| 测试项         | 标准                         | 测试方法                        |
| -------------- | ---------------------------- | ------------------------------- |
| **击杀时长**   | 250 关：4.5~5.2 秒           | 用 250 级账号实测 100 次取平均  |
| **生存时长**   | 250 关：≥45 分钟无操作       | 挂机测试脚本                    |
| **经济循环**   | 1-250 关总盈余 33%~37%       | 模拟玩家从 1 级打到 250 关      |
| **无尽可持续** | 巅峰 200 级玩家可挑战 450 关 | 压力测试：连续通关 100 关不卡顿 |
| **数值安全**   | 500 关金币<2^53 (9e15)       | 检查所有数值字段类型            |
